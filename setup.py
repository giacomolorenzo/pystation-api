from setuptools import find_packages, setup
setup(
    name='pystationapi',
    packages=find_packages(),
    version='0.1.0',
    description='Library to search a game in playstation store and return in json format',
    author='Giacomo Lorenzo',
    license='Apache License 2.0',
)